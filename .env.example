# API
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173

# Session + queue limits
SESSION_IDLE_TIMEOUT_MS=300000
QUEUE_MAX_PARTICIPANTS=250

# Optional local persistence file for session/queue state
SESSION_STORE_FILE=.data/session-store.json
